<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <artifactId>polaris-framework-starter-zuul</artifactId>
    <name>Nepxion Polaris Framework Starter Zuul</name>
    <packaging>jar</packaging>
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.nepxion.polaris</groupId>
        <artifactId>polaris-framework</artifactId>
        <version>1.0.0</version>
    </parent>

    <dependencies>
        <!-- 旗标核心模块 -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>polaris-module-starter-banner</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- 注册发现核心模块 -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>polaris-module-starter-discovery</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- 配置核心模块 -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>polaris-module-starter-config</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- 调用链核心模块 -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>polaris-module-starter-tracing</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- 指标核心模块 -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>polaris-module-starter-metrics</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- 防护核心模块 -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>polaris-module-starter-protector-zuul</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- 灰度蓝绿核心模块 -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>polaris-module-starter-gray-zuul</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>
</project>